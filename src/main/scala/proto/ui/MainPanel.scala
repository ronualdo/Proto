package proto.ui

import swing.FlowPanel

import java.awt.Color

import proto.Proto
import proto.Renderer

object MainPanel extends FlowPanel with Renderer {
  background = Color.BLACK
  
  def render(proto: Proto) {
    
  }

  def show() {
  
  }

}
